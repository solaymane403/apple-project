[build]
  command = "npm run build"
  publish = "dist"

# Minimal Netlify configuration.
# If you need environment variables or more settings later, add them carefully
# using proper TOML syntax (no trailing commas, unique table names, etc.).
