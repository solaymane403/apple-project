[build]
  command = "npm run build"
  publish = "dist"

[context.production.environment]
  # Example: specify node version if needed
  # NODE_VERSION = "18"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
  force = true
