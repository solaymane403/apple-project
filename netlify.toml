[build]
  command = "npm run build"
  publish = "dist"

# Minimal Netlify configuration. If you need environment variables or redirects,
# add them back carefully (no trailing commas, use proper TOML syntax).
